name: Post-merge Assurance

# Expensive build assurance runs only after code lands on main.
# Keep PR/merge-queue checks lean; do release-grade verification here.
permissions: {}

on:
  push:
    branches: [main]

concurrency:
  group: post-merge-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_INCREMENTAL: "0"
  CARGO_NET_GIT_FETCH_WITH_CLI: "true"
  CGO_ENABLED: "0"
  GOFLAGS: "-trimpath"
  RUST_VERSION: "1.94.0"
  GO_VERSION: "1.25.10"
  GOSEC_VERSION: "v2.25.0"
  # Opt into Node.js 24 now ahead of the June 2, 2026 forced migration
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  gosec-main:
    name: gosec (SAST, every main commit)
    # Scorecard's SAST check walks the last ~30 commits on the default branch
    # looking for a SAST run attached to each one. ci.yml only runs on
    # pull_request + merge_group, so squashed merge commits on main have no
    # gosec run associated. Running gosec here on every push to main keeps
    # the Scorecard SAST signal at 10/10. The flags and exclusion comments
    # below MUST stay in sync with ci.yml's gosec step.
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: "1.25.10"
          cache: false  # no external Go deps → no go.sum → caching not applicable

      - name: Install gosec
        run: go install "github.com/securego/gosec/v2/cmd/gosec@${GOSEC_VERSION}"

      - name: Run gosec (static security analysis)
        run: |
          # Exclude rules that are architectural false positives for a security CLI:
          #   G104: unhandled errors in cleanup/defer paths (intentional)
          #   G115: int conversions for fd values and bounded payload lengths (safe)
          #   G204: subprocess launch with variable (sir launches mister-core by design)
          #   G301: MkdirAll perms >0750 — Claude Code expects ~/.claude at 0755
          #   G302: Chmod >0600 — 0700 is correct for directories (execute bit needed)
          #   G304: file read via variable (sir reads config/state files by computed path)
          #   G306: WriteFile >0600 — settings.json needs 0644 for Claude Code compat
          #   G703: path traversal via taint — restore functions write to controlled paths
          gosec -quiet \
            -exclude=G104,G115,G204,G301,G302,G304,G306,G703 \
            ./...

  go-native-platforms:
    name: Go native / ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux_amd64
            runner: ubuntu-24.04
          - target: linux_arm64
            runner: ubuntu-24.04-arm
          - target: darwin_amd64
            runner: macos-15-intel
          - target: darwin_arm64
            runner: macos-15
          - target: windows_amd64
            runner: windows-2025
          - target: windows_arm64
            runner: windows-11-arm
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: "1.25.10"
          cache: false

      - name: Ensure Linux namespace prerequisites
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail

          missing=()
          for bin in unshare mount; do
            if ! command -v "${bin}" >/dev/null 2>&1; then
              missing+=("${bin}")
            fi
          done

          if [ "${#missing[@]}" -gt 0 ]; then
            sudo apt-get update
            sudo apt-get install -y util-linux
          fi

          uname -a
          command -v unshare
          command -v mount
          if sudo sysctl kernel.unprivileged_userns_clone >/dev/null 2>&1; then
            sudo sysctl -w kernel.unprivileged_userns_clone=1
          fi
          if sudo sysctl kernel.apparmor_restrict_unprivileged_userns >/dev/null 2>&1; then
            sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
          fi

      - name: Build sir
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p bin
          out=bin/sir
          if [ "${RUNNER_OS}" = "Windows" ]; then
            out=bin/sir.exe
          fi
          CGO_ENABLED=0 go build \
            -trimpath \
            -ldflags="-s -w" \
            -o "${out}" \
            ./cmd/sir

      - name: Test
        shell: bash
        run: |
          set -euo pipefail
          if [ "${RUNNER_OS}" = "Windows" ]; then
            # Full Go coverage runs on Linux. Keep Windows native assurance on
            # packages whose tests do not rely on POSIX permissions, /tmp paths,
            # or HOME semantics that differ from Windows runner behavior.
            go test \
              ./pkg/detect \
              ./pkg/friction \
              ./pkg/relay \
              ./pkg/runtime \
              ./pkg/secretscan \
              ./pkg/secretview \
              ./pkg/signal \
              ./pkg/telemetry
          else
            go test ./...
          fi

  artifacts:
    name: Build artifacts & checksums
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Install Rust toolchain
        run: |
          rustup toolchain install "${RUST_VERSION}" --profile minimal
          rustup default "${RUST_VERSION}"
          rustc --version
          cargo --version

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: "1.25.10"
          cache: false  # no external Go deps → no go.sum → caching not applicable

      - name: Build release artifacts
        run: |
          # Rust
          CARGO_INCREMENTAL=0 cargo build --release --locked

          # Go
          mkdir -p bin
          CGO_ENABLED=0 go build \
            -trimpath \
            -ldflags="-s -w" \
            -o bin/sir \
            ./cmd/sir

          # Copy Rust binary to bin/
          cp target/release/mister-core bin/ 2>/dev/null || true

      - name: Verify reproducible build
        run: |
          # Build a second time and compare hashes to verify determinism.
          # Same flags, same toolchains — output must be identical.
          sha256sum bin/sir bin/mister-core 2>/dev/null | sort > /tmp/build1.sha256 || true

          # Rebuild Go
          CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o bin/sir ./cmd/sir
          # Rebuild Rust
          CARGO_INCREMENTAL=0 cargo build --release --locked
          cp target/release/mister-core bin/ 2>/dev/null || true

          sha256sum bin/sir bin/mister-core 2>/dev/null | sort > /tmp/build2.sha256 || true

          if ! diff /tmp/build1.sha256 /tmp/build2.sha256; then
            echo "FATAL: Build is not reproducible — hashes differ between runs"
            echo "Build 1:" && cat /tmp/build1.sha256
            echo "Build 2:" && cat /tmp/build2.sha256
            exit 1
          fi
          echo "Verified: build is reproducible (hashes match)"

      - name: Generate checksums
        run: bash scripts/checksum.sh ./bin

      - name: Generate SBOM
        env:
          SYFT_VERSION: "1.9.0"
          SYFT_SHA256: "4c7db3f86a7cd86997fc37da412ac4c6031bb8d0a319a0b152791e53cd240ac4"
        run: |
          # Install syft at pinned version with checksum verification
          # (matches release.yml pattern — no curl|sh)
          curl -fsSLO \
            "https://github.com/anchore/syft/releases/download/v${SYFT_VERSION}/syft_${SYFT_VERSION}_linux_amd64.tar.gz"
          echo "${SYFT_SHA256}  syft_${SYFT_VERSION}_linux_amd64.tar.gz" | sha256sum --check
          tar -xzf "syft_${SYFT_VERSION}_linux_amd64.tar.gz" syft
          chmod +x syft
          ./syft version

          # Generate SBOM
          ./syft dir:. -o cyclonedx-json > sbom-sir.cdx.json
          echo "SBOM generated: sbom-sir.cdx.json"

      - name: Upload checksums
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: checksums
          path: |
            CHECKSUMS.sha256
            CHECKSUMS.sha512
            sbom-sir.cdx.json

      # Cosign signing is active in release.yml (keyless via Sigstore OIDC).
      # CI artifacts are unsigned — only release archives are signed.
